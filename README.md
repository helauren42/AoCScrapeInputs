# AoC Inputs Scraper

Create a secrets.txt file with your active AoC session cookie, pass the year to download as argument to the py script and voila!
A subdirectory per Day in the directory in which the script is run, with the input text in text files ready for you to parse.
There is an empty .py file and a main.cpp with base code to parse the input file created for every Day.
